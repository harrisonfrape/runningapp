[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Runs the App Router (pages, API routes, middleware) on Netlify Functions.
[[plugins]]
  package = "@netlify/plugin-nextjs"
